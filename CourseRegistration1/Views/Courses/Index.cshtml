@model IEnumerable<CourseRegistration1.Models.Course>

@{
    ViewData["Title"] = "Index";
}

<h1>Courses</h1>

<p>
    <a asp-action="Create">Create New Course</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.C_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.C_num)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.C_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.C_descr)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.C_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.C_num)
            </td>
            <td>
                <span style="cursor: pointer"
                      data-bs-toggle="modal" 
                      data-bs-target="#exampleModal"
                      
                >
                    @Html.DisplayFor(modelItem => item.C_name)
                </span>
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.C_descr)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.C_id }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>

<!-- Modal -->
<div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h1 class="modal-title fs-5" id="exampleModalLabel">Modal title</h1>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                ...
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>
<script>
    function showStudents(sName) 
    {
        $('#exampleModalLabel').html(C_name);
        cosnt URL = "@Url.Action("GetStudentsByCourseName", "Students")/" + sName;
        $.get(url.fuction(data)
        {
            console.log(url);
        });
        
    }
</script>

